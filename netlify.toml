[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# Let client-side routes (like /dashboard) load index.html directly.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
